generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Proposal {
  id String @id @default(uuid())
  title String 
  image String
  threshold Int
  allowance Int
  description String 
}